const subjects = ['Android', 'Java', 'iOS', 'Javascript', 'NodeJs'];

// subjects.push('React', 'Mongo');
// subjects.pop();
// subjects.shift();
// subjects.unshift('Native');

// subjects.splice(2, 2);
// subjects.splice(2, 0, 'PHP', 'Python');

console.log(subjects.indexOf('iOS'));
// subjects = [];
// console.log(subjects);
// console.log(subjects[2]);
// subjects[3] = 'Angular';
// console.log(subjects);
